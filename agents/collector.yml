receivers:
  prometheus:
    config:
      global:
        scrape_interval: 1m
        scrape_timeout: 10s
        
      scrape_configs:
      - job_name: 
        sample_limit: 10000

exporters:
  otlp:
    endpoint: "http://apm-server:8200"
    insecure: true